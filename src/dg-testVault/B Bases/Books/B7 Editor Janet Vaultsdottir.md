---
title: Janet Vaultsdottir
---

Intentionally UNPUBLISHED note (no `dg-publish`). It exists in the vault so
the plugin resolves `Editor` links to this note's full vault path, but the
published site never receives it — base views must render references to it
as a styled dead link labeled with the note name, exercising the
vault-present-but-unpublished path end to end.
